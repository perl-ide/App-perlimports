use strict;
use warnings;

my $file = new File $path, $data;
my $conn = connect Database $dsn;
my $json = JSON::PP->new;

use Unused;
no Forbidden;
require Deferred;

package Ignored;

sub Widget { }
